{
  "name": "content-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline browser editing kit for static and i18n websites.",
  "license": "MIT",
  "bin": {
    "content-kit": "./bin/content-kit.mjs"
  },
  "files": [
    "bin/",
    "src/",
    "content-tool.mjs",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "init": "node bin/content-kit.mjs init",
    "apply-edits": "node bin/content-kit.mjs apply-edits",
    "check": "npm run format:check && npm run test:unit && node scripts/check.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run check",
    "test:unit": "node --test",
    "pack:check": "npm pack --dry-run"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "prettier": "3.8.3"
  }
}
